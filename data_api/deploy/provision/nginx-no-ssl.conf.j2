# Config is autogenerated – do not change it manually
server {
    listen 80;
    listen [::]:80;

    server_name {{ http_domain }};

    location / {
        root /var/www/{{ http_domain }};
    }
}
